# per-cep-tor
This project builds a Perceptron from scratch using NumPy for binary classification. It trains on generated data, updates weights using the learning rule, predicts labels, evaluates accuracy, and visualizes the linear decision boundary separating two classes.
